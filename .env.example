# Copy to .env. Needed for POST /api/admin/reload; leave empty to disable reloads.
ADMIN_TOKEN=
# Frontend origin allowed by CORS (comma-separated for several).
CORS_ORIGIN=http://localhost:8080
RATE_LIMIT_PER_MINUTE=60
